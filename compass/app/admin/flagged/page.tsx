import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Moderation Settings',
};

export default function Page() {
  return <p>All the flagged reviews, requests will appear here.</p>;
}